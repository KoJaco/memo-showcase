const Showcase = () => {
    return (
        <div className="w-full min-h-[600px] bg-foreground/10 animate-pulse rounded-lg items-center justify-center flex">
            Showcase
        </div>
    );
};

export default Showcase;
